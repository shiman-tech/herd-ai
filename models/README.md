Optional alternate model directory.

If you place your model here:

`models/cow_identifier.tflite`

the app can load it as a fallback path.

Preferred location remains:

`assets/models/cow_identifier.tflite`
